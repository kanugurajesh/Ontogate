# Optional. When set, `ontogate run` automatically uses the LLMPlanner (OpenAI
# Chat Completions, grounded in the live ontology + tool registry). Without
# it, the runtime falls back to the deterministic RuleBasedPlanner - no key,
# no network, and fully reproducible for tests/demos.
OPENAI_API_KEY=
